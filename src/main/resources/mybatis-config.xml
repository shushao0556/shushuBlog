<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--引入数据库连接配置 -->
    <properties resource="database.properties"/>
    <!--定义别名-->
    <typeAliases>
        <package name="com.evshou.entity"/>
    </typeAliases>
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor"/>
    </plugins>
    <!--定义数据源-->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="BlogUsersMapper.xml"/>
        <mapper resource="BlogOptionsMapper.xml"/>
        <mapper resource="BlogPostsCategoryMapper.xml"/>
        <mapper resource="BlogCommentsMapper.xml"/>
        <mapper resource="BlogLinksMapper.xml"/>
        <mapper resource="BlogPostsLogMapper.xml"/>
        <mapper resource="BlogPostsMapper.xml"/>
    </mappers>
</configuration>